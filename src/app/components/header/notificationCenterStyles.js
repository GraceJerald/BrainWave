const styles = (theme) => ({
  appBar: {
    position: 'relative',
  },
  title: {
    marginRight: 'auto',
  },
  accent: {
    backgroundColor: theme.palette.secondary['A200'],
  },
});

export default styles;
